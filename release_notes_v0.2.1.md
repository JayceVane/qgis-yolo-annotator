# qgis_yolo_annotator v0.2.1

自 v0.2.0 以来的缺陷修复，建议所有用户升级。

## 修复

- **多在线场景导出同名互相覆盖**：同一在线图层的多个场景导出文件名相同，互相覆盖只产出一张；现导出文件名带场景名（scene_001.tif …）
- **XYZ 图层探测失败误报「场景范围过小」**：图层 source 参数顺序变化时探测不到在线图层，导致画场景误走文件分支；现参数顺序无关识别，且 xyz 模式不再误落文件分支
- **导出对话框崩溃**：ExportDialog 缺 QWidget 导入，NameError（检查脚本已并入 pyflakes 未定义名扫描防复发）

## v0.2.0 tag 之后新增

- 场景矩形中心显示场景名标注（白字黑边），便于识别当前标注的场景
- 未推理/未加载场景时「画 OBB」自动恢复场景上下文（纯手工标注无需先跑推理）
- 手工画 OBB 后场景状态自动联动：未标注 → 已标注（只升级不降级）
- 新建/打开工程时清空旧工程在画布上的场景框/标注框/栅格图层

## 安装

QGIS 插件管理器 → 从 ZIP 安装，选择 `qgis_yolo_annotator-0.2.1.zip`；或解压到 QGIS profile 的 `python/plugins/` 目录。
